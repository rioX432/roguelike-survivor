# Session Memory — 2026-02-23

## Project: GlitchClaw (Roguelike Survivor)

### State at 13:50 JST

#### Phase 1–4: COMPLETE (32 scripts, 13 PRs merged)
All core C# scripts are in `roguelike-survivor-game/Assets/Scripts/`.

#### Current Problem Being Solved
Scene is empty (no GameObjects, no Inspector refs). Approach: `GameBootstrap.cs` — a runtime
self-initializing MonoBehaviour that creates ALL scene objects, links ALL references in code,
requires zero Inspector setup.

#### Scripts Written/Modified This Session

**NEW:**
- `Assets/Scripts/Core/GameBootstrap.cs` — Full runtime bootstrap (creates managers, map, player,
  camera, HUD, enemies, joystick, level-up panel, result screen entirely in code)

**MODIFIED (Inject methods added):**
- `PlayerController.cs` → `InjectStats(PlayerStats)`, `InjectJoystick(VirtualJoystick)`
- `VirtualJoystick.cs` → `InjectRects(RectTransform bg, RectTransform handle, float range)`
- `CameraFollow.cs` → `InjectTarget(Transform)`
- `AttackModule.cs` → `SetData(WeaponData)`
- `HPBar.cs` → `InjectSlider(Slider)`, `InjectFillImage(Image)`
- `XPBar.cs` → `InjectSlider(Slider)`, `InjectLevelText(TMP_Text)`
- `TimerUI.cs` → `InjectText(TMP_Text)`
- `ResultScreen.cs` → `InjectUI(GameObject, TMP_Text, TMP_Text, TMP_Text, Button)`
- `LevelUpUI.cs` → `InjectPanel(GameObject, List<Button>, List<TMP_Text>)`;
  fixed `AddOrUpgradeModule` to call `SetData(data)` after AddComponent
- `LevelUpSystem.cs` → `SetWeapons(List<WeaponData>)`
- `EnemyBase.cs` → `SetXPGemPrefab(GameObject)`
- `EnemySpawner.cs` → `InjectRefs(Transform, SpawnTableData, List<EnemyPrefabEntry>)`;
  fixed `SpawnEnemy` to call `eb.Initialize(data, _player)` after spawning (was missing!)
- `InfiniteMapController.cs` → `SetPlayer(Transform)`, `SetTile(Sprite)`;
  moved `BuildChunkGrid()` from Awake → Start (so SetTile can be called first)
- `Projectile.cs` → `RegisterPrefab(GameObject)` static method (was auto-set on Start which
  didn't work for inactive prefabs)

#### Known Architecture Details
- `Projectile.PrefabInstance` static — must call `Projectile.RegisterPrefab(prefab)` explicitly
- `EnemySpawner` uses `SpawnTableData` (SO) with `List<WaveEntry>` — create at runtime via
  `ScriptableObject.CreateInstance<SpawnTableData>()`
- `InfiniteMapController` uses `SpriteDrawMode.Tiled` — needs `_backgroundTile` Sprite set before Start
- `LevelUpUI` auto-finds Player by tag in Start(); shows 3 upgrade cards from `_availableWeapons`
- `LevelUpSystem` event-driven: listens to `EventBus.OnXPCollected`, `OnEnemyDeath`
- `GameBootstrap` execution order: `[DefaultExecutionOrder(-200)]`

#### GameBootstrap Status
Written but NOT YET TESTED. Still needs:
1. Verify compilation (no errors)
2. Confirm it properly wires EnemySpawner (SpawnTableData + EnemyPrefabEntry list)
3. Confirm VirtualJoystick InjectRects is compatible with the existing VirtualJoystick.cs fields
4. Rebuild APK + install on device + smoke test

#### Unity / Build Info
- Unity exe: `/Users/hackathon/workspace/6000.3.9f1/Unity.app/Contents/MacOS/Unity`
- Project: `/Users/hackathon/workspace/roguelike-survivor/roguelike-survivor-game/`
- APK output: `roguelike-survivor-game/Library/Bee/Android/Prj/IL2CPP/Gradle/launcher/build/outputs/apk/release/launcher-release.apk`
- App: `com.DefaultCompany.roguelikesurvivorgame` / `com.unity3d.player.UnityPlayerGameActivity`
- ADB device: `adb-R5CY12JYCPW-8NxypE._adb-tls-connect._tcp` (Galaxy S25 Ultra)
- Claude Code: `~/.nvm/versions/node/v22.22.0/bin/claude` (v2.1.50)
- GitHub: `rioX432/roguelike-survivor`

#### Next Actions
1. Check VirtualJoystick.cs has `_background`, `_handle`, `_range` fields for InjectRects to work
2. Confirm EnemySpawner SpawnTableData field name is `waves` (List<WaveEntry>)
3. Build: run batchmode Unity build
4. Install APK: `adb install -r <path>`
5. Smoke test on device
